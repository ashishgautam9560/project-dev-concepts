server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: "*"

spring:
  datasource:
    url: jdbc:mysql://localhost:3306
    username: root
    password: 1234
  output:
    ansi:
      enabled: ALWAYS
  data:
    redis:
      host: localhost
      port: 6379

logging:
  level:
    org:
      springframework:
        jdbc:
          core: DEBUG
    

    
    